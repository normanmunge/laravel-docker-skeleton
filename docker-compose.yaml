version: '3.8'

services:
  server:
    image: 'nginx:stable-alpine'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - '8000:80'
    environment:
      - name=value
  # php:
  # mysql:
  # composer:
  # artisan:
  # npm:
